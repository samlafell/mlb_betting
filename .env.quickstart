# 🚀 MLB Betting System - Quick Start Configuration
# 
# This is a MINIMAL configuration template for getting started quickly.
# Only includes essential settings needed for basic functionality.
# 
# For production deployment, use the full .env.example instead.
# 
# Usage: Copy this file to .env and run ./quick-start.sh

# ==========================================
# 🗄️ DATABASE (Required)
# ==========================================
# These values work with the quick-start Docker containers
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=mlb_betting
POSTGRES_USER=samlafell
POSTGRES_PASSWORD=quickstart_dev_password_change_for_production

# ==========================================
# 🔄 REDIS (Required for caching)
# ==========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# ==========================================
# 🌍 ENVIRONMENT (Required)
# ==========================================
ENVIRONMENT=development
LOG_LEVEL=INFO
DEBUG=false

# ==========================================
# 🚀 API (Optional - only needed for web interface)
# ==========================================
API_HOST=0.0.0.0
API_PORT=8000
API_SECRET_KEY=quickstart-development-key-please-change-for-production

# ==========================================
# 📊 MONITORING (Optional but recommended)
# ==========================================
ENABLE_METRICS=true

# ==========================================
# 🚨 SECURITY NOTICE
# ==========================================
# This configuration is for DEVELOPMENT ONLY
# For production:
# 1. Use strong, unique passwords
# 2. Enable SSL/TLS
# 3. Configure proper security headers
# 4. See .env.example for complete security options